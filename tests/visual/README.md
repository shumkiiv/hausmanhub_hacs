# Визуальная регрессия панели HausmanHub

`hausman-hub-panel-harness.html` загружает панель с фиксированными тестовыми
данными. Он нужен для проверки интерфейса без запущенного Home Assistant и без
изменения реальной конфигурации дома.

## Запуск

Из корня репозитория запустите статический сервер:

```bash
python -m http.server 8765
```

Базовый адрес:

```text
http://127.0.0.1:8765/tests/visual/hausman-hub-panel-harness.html
```

Поддерживаемые параметры:

- `section`: `overview`, `lighting`, `climate`, `rooms`, `media`, `security`,
  `devices`, `energy`, `scenarios`, `settings`;
- `screen` для раздела климата: `contour`, `profiles`, `schedule`, `home`,
  `windows`, `assistant`, а также `wizard`, `first-run-room`, `first-run-home`,
  `first-run-validation` для проверки мастера первого запуска;
- `settings` для раздела настроек: `overview`, `rooms`, `connection`,
  `appearance`, `system`;
- `theme`: `light` или `dark`.
- `openScenario`: открыть редактор сценария по части его названия.
- `figmaCapture=1`: явно подключить скрипт импорта Figma; без параметра стенд
  полностью локален и не загружает внешний JavaScript.

Стенд содержит локальный сохраняемый имитатор API сценариев и панели. Поэтому
можно без воздействия на реальный дом проверить создание, запуск, избранное,
редактирование, удаление и повторную загрузку сценария. Все вызовы доступны в
`window.__hausmanHubHarnessCalls`, а ошибки — в `window.__hausmanHubHarnessErrors`.

Пример:

```text
http://127.0.0.1:8765/tests/visual/hausman-hub-panel-harness.html?section=climate&screen=profiles&theme=dark
```

## Обязательная матрица

| Состояние | Размер окна |
| --- | --- |
| Обзор, светлая тема | 1440 × 1200 |
| Обзор, тёмная тема | 1440 × 1200 |
| Освещение | 1440 × 1200 |
| Комнаты | 1440 × 1200 |
| Медиа | 1440 × 1200 |
| Безопасность | 1440 × 1200 |
| Устройства | 1440 × 1200 |
| Энергия и детализация источника | 1440 × 1200 |
| Климат: контур | 1440 × 1400 |
| Климат: профили | 1440 × 1500 |
| Климат: расписание | 1440 × 900 |
| Климат: сигналы дома | 1440 × 1800 |
| Климат: сигналы комнат | 1440 × 1800 |
| Климат: Умный помощник | 1440 × 1700 |
| Сценарии | 1440 × 900 |
| Киоск | 1440 × 900 |
| Первый запуск: комната / дом / проверка | 1440 × 1200 |
| Первый запуск: комната, граница широкого меню | 1280 × 900 |
| Первый запуск: комната, до границы меню | 1049 × 900 |
| Первый запуск: комната, после границы меню | 1050 × 900 |
| Настройки | 1440 × 1400 |
| Все основные разделы, средний экран | 1280 × 900 |
| Все основные разделы, планшетная ширина | 900 × 900 |
| Все основные разделы, компактный экран | 640 × 1000 |
| Профили, узкий экран | 600 × 1600 |
| Настройки, узкий экран, тёмная тема | 600 × 1600 |

Снимки являются временными артефактами проверки и не добавляются в Git.

## Критерии приёмки

- одновременно отображается только выбранный экран климатического раздела;
- на широком экране нет горизонтальной обрезки текста и элементов;
- на узком экране горизонтальная прокрутка допустима только в подменю климата;
- заголовки, значения, поля и кнопки не перекрываются и не выходят из карточек;
- светлая и тёмная темы сохраняют читаемый контраст и одинаковую иерархию;
- в пользовательских подписях нет необработанных идентификаторов API или
  строк вида `mdi:*`, кроме технических полей выбора сущностей;
- физическое устройство считается один раз, независимо от количества его
  функций и сущностей Home Assistant;
- интерфейс соответствует структуре экранов файла Figma `HMH HA`.
