# HACS 1.52.147: плотность интерфейса и production deploy

Дата: 2026-08-22
Агент: Codex

## Релиз

- В main перенесены frontend commits `305c83c`, `ddd6d6e`, `5a2387f`.
- Release commit/tag: `aa4ef59`, `v1.52.147`.
- GitHub Release опубликован без assets.
- Полный локальный gate: 1609 passed, 4 skipped. UI-профиль: 102 passed,
  50 subtests.
- GitHub Actions `32576732490` завершён со статусом success.

## Production

- Preflight и post-restart config checks: HTTP 200.
- Full backup `aab91cba`: по 921815040 байт в `hassio.local` и
  `hassio.KeeneticSSD`, обе копии защищены, ошибок агентов нет.
- Установлена явная версия `v1.52.147`. Home Assistant перезапущен ровно
  один раз и восстановился в состоянии `RUNNING`.
- Update entity: installed/latest `v1.52.147`, in_progress false.
- Config entry `HausmanHub` loaded. Dashboard и upcoming API отвечают
  HTTP 200, список содержит 11 ближайших событий.
- Operation journal остался на sequence 998. Climate guard сохранил monitor
  revision 1 и не вооружён. Deploy не добавил операций или guard events.
- Все 88 production JS/CSS-файлов совпадают с release по SHA-256. В system
  log нет ошибок Hausman.
- Физические команды не отправлялись. Android, backend, API, contracts и
  storage не менялись.

## Откат

- Восстановить full backup `aab91cba`.
