# Code review незакоммиченных HASC-изменений

- Session: `ses_09a5eeb72ffeXfHaOwwcLdSfdX`
- Agent: `ivsh-plan`
- Project: `/home/ivsh/projects/hausmanhub_hasc`

## 2026-07-15T14:54:43+03:00 - user - user-prompt

- Part: `prt_f65a114f5001JlUPMtJFdb8DK2`
- Message: `msg_f65a114ec001cWUp2n4gkk555s`

"Проведи обязательный независимый code review только текущих НЕЗАКОМИЧЕННЫХ изменений в /home/ivsh/projects/hausmanhub_hasc. Сначала прочитай AI_CONTEXT.md и git diff. Цель: замкнуть только учебный жизненный цикл. После новой безопасной HASC-установки в третьем пустом Core она удаляется, её локальная страница для временного точного read-only пользователя отвечает 503 без count-ключей, внешняя временная запись не меняется, затем четвёртый пустой Core с тем же временным каталогом подтверждает полное отсутствие HASC. Проверь порядок, все ли остатки/состояния/маршрут покрыты, безопасное создание временного пользователя, отсутствие чтения/печати чисел, сохранность внешней записи, совместимость Core 2026.6.4/2026.7.0, чистый код и границы без новых данных/прав. Никаких настоящих HA, домашней сети, живых данных, команд, сервисов или управления. Ничего не редактируй, не добавляй в индекс, не делай commit/push, не используй reset/checkout и не управляй службами. Разрешены только чтение и локальные проверки. В конце дай краткий отчёт по-русски и закончи строго NO FINDINGS, если замечаний нет, иначе перечисли точные замечания."

## 2026-07-15T14:56:38+03:00 - assistant - progress-note

- Part: `prt_f65a2ac48001euBs0xQ9wvzTlC`
- Message: `msg_f65a276c8001zWfnMjPbmasQFm`

**Code review: незакоммиченные изменения в `hausmanhub_hasc`**

Проверены только текущие unstaged-изменения в трёх файлах:

- `docs/read-only-skeleton.md`
- `tests/test_read_only_skeleton.py`
- `tools/check_home_assistant_core.py`

Цель изменений — замкнуть учебный жизненный цикл: после новой безопасной HASC-установки в третьем пустом Core интеграция удаляется, её локальная страница возвращает 503 без count-ключей, внешняя временная запись остаётся неизменной, затем четвёртый пустой Core подтверждает полное отсутствие HASC.

**Что проверено:**

1. **Порядок операций** — в `async_run_check()` последовательность верная: третий пустой экземпляр (`post_removal_hass`) → fresh setup → удаление → проверка 503 → неизменность внешней записи → четвёртый пустой экземпляр (`final_hass`) → подтверждение полного отсутствия HASC.

2. **Покрытие остатков / состояний / маршрутов** — `assert_hasc_stays_removed_after_restart()` проверяет отсутствие:
   - config entries,
   - services,
   - runtime data,
   - local summary route,
   - entities,
   - devices,
   - state values,
   - и при этом сохраняет внешнюю запись неизменной.

3. **Безопасное создание временного пользователя** — используется `async_create_test_read_only_access_token()`, которая создаёт `local_only=True` пользователя с группой `GROUP_ID_READ_ONLY` и проверяет `is_admin is False`.

4. **Отсутствие чтения/печати чисел** — проверка 503 (`async_assert_local_summary_is_unavailable_after_removal`) только отвергает любой ключ из `SUMMARY_SENSOR_KEYS` в ответе; не читает и не выводит значения. Единственный `print` — финальное сообщение об успехе.

5. **Сохранность внешней записи** — после fresh-удаления вызывается `assert_reserved_collision_entry_is_unchanged()`, и та же проверка есть внутри финального `assert_hasc_stays_removed_after_restart()`.

6. **Совместимость Core 2026.6.4/2026.7.0** — добавленные вызовы используют уже существующие API (`async_create_test_read_only_access_token`, `async_remove_safe_entry`, `async_start_empty_home_assistant`, `assert_hasc_stays_removed_after_restart`), никаких новых зависимостей или API-методов не появилось.

7. **Чистый код и границы** — изменения не добавляют новых runtime-возможностей, новых данных или прав. Это только тестовые утверждения и документация, соответствующие существующим паттернам.

8. **Локальные проверки** — запущен `python3 -m unittest discover -s tests -v`: 73 теста успешно.

**Вывод:** замечаний нет.

NO FINDINGS

