# HACS 1.52.134: ширина главной, release и deploy

Дата: 2026-08-21
Агент: Codex
Репозиторий: `hausmanhub_hacs`
Ветка: `kimi/hacs-tablet-parity-2026-08-20`

## Итог

- Причина жалобы на растяжение: production продолжал загружать панель
  `v1.52.133`, а локальная правка ширины ещё не была выпущена.
- Главная получила общий с обычными страницами предел ширины 1600 px,
  центрирование и поля 34 px. Внутренняя планшетная сетка сохранена.
- Feature commit `a1e06e8`, release commit/tag `287f1e4`, `v1.52.134`.
- Release:
  https://github.com/shumkiiv/hausmanhub_hacs/releases/tag/v1.52.134.
- GitHub Actions `32514457615` завершён успешно.

## Проверка и deploy

- Staged release-gate: 1547 tests, 4 skipped.
- Перед deploy создан защищённый full backup `2ac3c237` в local и
  KeeneticSSD.
- HACS установлен явной `v1.52.134`, `check_config` успешен, Home Assistant
  перезапущен один раз.
- После restart installed/latest `v1.52.134`; все 28 изменённых frontend
  assets совпадают с release, 10 сущностей доступны, ошибок Hausman Hub нет.
- На production viewport 2560x1306 главная и «Освещение» имеют одинаковую
  ширину `main` 1600 px и центрируются. Runtime errors отсутствуют.
- Физические команды дому не отправлялись. Откат: backup `2ac3c237`.

## Границы задачи

- Android, API и contracts не менялись.
- Release построен на актуальном `origin/main`, где уже находился backend
  commit `4c79d8d`. Эта задача backend не редактировала.
