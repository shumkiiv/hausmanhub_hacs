# Финальное ревью staged diff HASC

- Session: `ses_09ef61444ffeZVKvvl2ckbUiNZ`
- Agent: `ivsh-plan`
- Project: `/home/ivsh/projects/hausmanhub_hasc`

## 2026-07-14T17:31:08+03:00 - user - user-prompt

- Part: `prt_f6109ed650016O2Pn7zHCFTXO2`
- Message: `msg_f6109ed5b001W62CavGjYqD6cF`

"Проведи обязательное финальное ревью staged diff. Контекст: отдельный публичный HASC репозиторий. Интеграция только для чтения и сравнения, без управления домом, сервисов Home Assistant, Node-RED, прокси, прямого выполнения, сетевых вызовов и секретов. Изменение добавляет локальную проверку перед публикацией: она читает только Git-рабочую копию или staged Git-данные, должна выявлять типичные ключи и runtime/backup файлы, не ломать проверку текущего репозитория и не обходить границы. Проверь корректность, безопасность, ложные срабатывания и пропуски в разумном объёме, чистоту кода, тесты и документацию. Перед ответом изучи staged diff и результаты: 44 unittest прошли; `python3 tools/check_repository_boundary.py`; `python3 tools/check_repository_boundary.py --staged`; `git diff --cached --check` прошли. Верни находки по важности либо явно напиши, что находок нет."

## 2026-07-14T17:36:46+03:00 - assistant - status-report

- Part: `prt_f610ee2d4001G74ssOZf6MWZ9N`
- Message: `msg_f610e9737001g47wJj97OnmOIl`

Статус: staged diff пока не готов к публикации — есть две находки высокой важности.

### Находки

1. **Высокая — чтение за пределами репозитория через symlink**  
   `tools/check_repository_boundary.py:88-96`  
   `checked_relative_path()` проверяет только компоненты пути, но `Path.read_bytes()` следует по символическим ссылкам, включая ссылки в родительских каталогах. Отслеживаемый symlink может заставить обычную проверку прочитать файл вне корня репозитория. Это нарушает заявленную границу чтения. Сценарий независимо воспроизведён Kimi-ревью.

2. **Высокая — пропускаются типичные runtime-файлы и резервные копии Home Assistant**  
   `tools/check_repository_boundary.py:20-22`, `161-185`  
   Не блокируются, например, `backups/full_backup.tar`, `home-assistant_v2.db`, `home-assistant.log` и `configuration.yaml`. Архивы и SQLite-файлы обычно содержат NUL, после чего проверка содержимого полностью отключается на строках 184–185. Это расходится с обещанием обнаруживать настройки и резервные копии Home Assistant в `docs/repository-safety-check.md:9-12`.

3. **Средняя — распространённые секреты в JSON не распознаются**  
   `tools/check_repository_boundary.py:24-27`  
   Шаблон требует незаключённое в кавычки имя поля в начале строки. Поэтому обычные записи `"access_token": "..."`, `"password": "..."` и однострочный JSON проходят проверку. Также не покрыты распространённые `client_secret` и `refresh_token`.

4. **Средняя — тесты не проверяют ключевые границы ввода**  
   `tests/test_repository_boundary.py:25-75`  
   Нет тестов фактического symlink-выхода, `staged_files()`, типичных HA backup/runtime-файлов и JSON-секретов. Проверка `../outside` покрывает только лексический путь и не обнаруживает главную проблему.

5. **Низкая — чрезмерно широкие ложные срабатывания**  
   `tools/check_repository_boundary.py:172`, `176-177`  
   Блокируется любой `.pem`, включая публичные сертификаты, и любой JSON с подстрокой `flow`: например, `workflow.json`, `overflow.json` или `flower.json`.

6. **Низкая — документация неточно описывает обычный режим**  
   `docs/repository-safety-check.md:20-24`  
   Написано, что проверяются «уже опубликованные файлы», но команда читает текущую рабочую копию отслеживаемых файлов, включая незакоммиченные изменения.

**Проверки:** 44 unittest, обе команды проверки границ и `git diff --cached --check` — успешно по предоставленным результатам; повторно не запускались. Kimi-backed `ivsh-review` частично завершился из-за лимита шагов, но успел независимо подтвердить symlink-проблему. Файлы не изменялись.

**Время:** начало 2026-07-14 17:31, окончание 2026-07-14 17:36, всего 0:05.

