# HACS 1.52.138: режимы главной и production deploy

Дата: 2026-08-22
Агент: Codex

## Релиз

- Feature commit `2921547`, context commit `57b30c4`, release commit/tag
  `1596a74`, `v1.52.138`.
- GitHub Release опубликован без assets.
- Local release-gate: 1563 tests, 4 skipped.
- GitHub Actions `32558198896` завершён со статусом success.

## Production

- Preflight config check: HTTP 200.
- Full backup `22410676`: по 919613440 байт в `hassio.local` и
  `hassio.KeeneticSSD`, Home Assistant, база, 10 add-ons и `ssl`, ошибок
  агентов нет.
- Установлена явная версия `v1.52.138`, повторный config check успешен,
  Home Assistant перезапущен ровно один раз.
- Update entity: installed/latest `v1.52.138`, in_progress false.
- Config entry loaded. Все 12 релевантных сущностей доступны.
- Admin panel, Dashboard, upcoming events и operation journal отвечают
  HTTP 200. Journal проходит JSON Schema.
- Все 29 изменённых frontend assets совпадают с release по SHA-256.
- В system log нет ошибок и traceback. Одно известное climate WARNING
  относится к расхождению физического уличного датчика и погодного сервиса.
- Физические команды не отправлялись.

## Откат

- Восстановить full backup `22410676`.
