# Аудит выпуска Hausman for Home Assistant 1.52.182

Дата: 2026-08-27.

## Результат

- Исправлен размер и выравнивание маленькой иконки в бейдже движка сценария.
  Значок больше не выходит на рамку и не наезжает на подпись.
- Tag `v1.52.182` и Release без assets указывают на `4b0852a`.
- GitHub Actions `33062946670` завершился успешно.
- Production Home Assistant использует installed/latest `v1.52.182`.

## Проверки до установки

- Local release gate: 1734 теста, 4 skipped.
- Browser gate: 19 Chromium-тестов, включая visual и accessibility.
- Геометрический тест проверяет внутренний размер иконки 15 px, поля 7 px,
  промежуток 5 px и вертикальное центрирование.
- Пакет HACS, версия, README и границы публичного репозитория проверены.

## Резервная копия и установка

- Повторно использован созданный в тот же день protected full backup
  `6d01ac53` в `hassio.local`, размер 882913280 байт.
- Версия установлена точно через update service с `backup=false`.
- Config check принят до restart, Core перезапущен один раз и штатно поднялся.

## Проверка production

- installed/latest равны `v1.52.182`, обновление не выполняется.
- В каталоге 53 сценария, все 53 включены; Scenario Health `healthy`.
- Node-RED 22.0.2 установлен, запущен и подключён; доступны два managed flow.
- Live CSS содержит `--mdc-icon-size:15px`, `--ha-icon-display:block` и
  ограничение переполнения. Основной JS использует cache version `1.52.182`.
- Read-only smoke прошёл, максимальная задержка API 105 мс. Физические команды
  дому не отправлялись.
- Десять служебных сущностей доступны. Две сущности телеметрии планшета после
  restart остаются `unavailable`, пока Android не отправит новый отчёт. В
  system log нет записей Hausman.

## Откат

Восстановить protected full backup `6d01ac53` либо точно установить
`v1.52.181`, выполнить config check и один restart.
