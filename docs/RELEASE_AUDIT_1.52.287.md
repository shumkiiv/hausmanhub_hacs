# Hausman Hub HACS 1.52.287: выпуск и установка

Дата: 2026-09-16. Объём: HH-006 и HH-007, безопасный дневной переход
тамбура и душевой.

## Состав

- Ремонт: `19cb985`.
- Выпуск: `292e31f`, tag `v1.52.287`.
- Публичный релиз: `https://github.com/shumkiiv/hausmanhub_hacs/releases/tag/v1.52.287`.

## Проверки до публикации

- Целевые: 203 passed, семь файлов освещения, контракт 0.70.0 (`8581cc9`).
- Полный pytest: 3269 passed, 4 skipped, 1297 subtests passed.
- Полный local release gate: 2363 unit tests, 4 skipped; затем полный pytest,
  fixtures, Android compatibility, версия, README, naming, HACS package и
  проверки границ репозитория PASS.
- Финальные независимые ревью и аудит: PASS. Внешний helper ревью не дал
  заключения, поэтому его успех не заявляется; выполнено независимое местное
  ревью.

## Установка

- До установки: installed/latest `v1.52.286`.
- Штатный `update.install` применил строго `v1.52.287` без backup.
- До и после перезапуска `ha core check` завершился успешно.
- После перезапуска update entity подтвердил installed/latest `v1.52.287`,
  `in_progress=false`; диагностические сущности интеграции доступны.
- JS и CSS панели доступны и содержат marker `1.52.287`.

## Ограничения

Физические команды не отправлялись. Ручная защита, ручная атрибуция душевой,
настройки, storage и Node-RED не менялись. Нужна суточная проверка разрешённых
переходов и обоих клиентов. До неё HH-006 и HH-007 VERIFIED, но не ACCEPTED.
