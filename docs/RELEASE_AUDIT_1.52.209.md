# Проверка выпуска 1.52.209

## Что входит

Финальная часть исправления общей температуры. После удаления комнаты без
исполнителя выбранной оси сервер теперь использует тот же сокращённый список
для room scope и exact target facts. Раньше эти три представления расходились,
и движок безопасно отклонял повторную проверку как внутренне
противоречивую.

Ручные и недоступные устройства остаются в исходной надёжной квитанции, но не
входят в физическую повторную проверку. Уже принятые команды соседних комнат
не теряются и не повторяются. Контракты `0.64.3` и Android API не менялись.
Выпускная provenance привязана к `1.52.209` и digest
`f1908aba6b40f15d719ba2b6e56f6ee1dd14afd55005aeff5fbe2f27e62e6435`.

## Проверено локально

- RED воспроизвёл `application plan facts are inconsistent` после удаления
  пустой комнаты;
- GREEN подтвердил единый scope только для комнат с исполнителем выбранной
  оси;
- профильный набор: 24 теста и 34 подпроверки;
- полный выпускной набор, coverage и browser-gate выполняются до публикации.
