# Аудит соответствия Android, Figma и HACS

Дата: 2026-08-01.

## Правило синхронизации

1. Экран проектируется и получает канонический `CANON`-фрейм в Figma.
2. Android и HACS ссылаются на один и тот же экран и отличаются только
   адаптацией ширины и платформенными системными элементами.
3. Устаревший вариант получает префикс `ARCHIVE /`; удаление выполняется только
   после отдельной проверки, что на него нет ссылок.
4. Изменение считается завершённым после проверки Figma-снимка, Android на
   планшете, HACS на 1440/1024/640 px и автоматических тестов.

## Текущее состояние

| Раздел | Android | Figma Android | Figma HACS | HACS | Статус |
|---|---|---|---|---|---|
| Энергия | 1.0.85 | dark `987:1368`, light `991:1368` | dark `247:582`, light `251:582` | 1.51.37 | синхронизирован |
| Главная | реализован | `716:943` | `276:11` | 1.51.58 | синхронизирован |
| Освещение | реализован | требуется инвентаризация | карточка устройства `281:676` | 1.51.58 | общий drill-down синхронизирован |
| Климат | реализован | требуется инвентаризация | несколько версий | реализован | следующий аудит |
| Комнаты | реализован | требуется инвентаризация | мастер и `281:676` | 1.51.58 | общий drill-down синхронизирован |
| Медиа | реализован | требуется инвентаризация | экран `237:582`, карточка `281:676` | 1.51.58 | общий drill-down синхронизирован |
| Безопасность | реализован | требуется инвентаризация | карточка устройства `281:676` | 1.51.58 | общий drill-down синхронизирован |
| Устройства | реализован | требуется инвентаризация | мастер и карточка `281:676` | 1.51.58 | общий drill-down синхронизирован |
| Сценарии | реализован | `661:645`, `689:380` | библиотека `255:582`, редактор `282:582` | 1.51.59 | мастер редактора синхронизирован |
| Настройки | реализован | несколько самостоятельных диалогов | несколько версий | реализован | требуется карта подразделов |

## Зафиксированные решения по Energy

- Фактический экран Android 1.0.85 на Lenovo TB336FU использован для возврата
  обоих Figma-файлов к одной актуальной компоновке 2048 × 1264 px.
- Иерархия в обеих платформах: четыре текущие метрики, история Recorder с
  периодами, выбранные источники, одна строка на физическое устройство, правая
  колонка состояния и настройка карточки.
- В `HMH II` и `HMH HA` зафиксированы попарно идентичные тёмный и светлый
  каноны. Контрольные PNG тёмных вариантов и отдельно светлых вариантов имеют
  одинаковые SHA-256 между файлами.
- Старые `930:1363` и `192:543` сохранены как исторические фреймы и больше не
  являются спецификацией реализации.
- API, модель устройств и backend в этой синхронизации не менялись.

## Единая карточка физического устройства

- Канонический component set HACS: `281:676`; варианты `Compact` (`280:586`) и
  `Detail` (`281:582`), документационный раздел `280:582`.
- Изображение берётся из Zigbee2MQTT. Если его нет или загрузка завершилась
  ошибкой, показывается нейтральный ассоциативный fallback, а не случайный датчик.
- Несколько сущностей Home Assistant одного прибора остаются возможностями одной
  карточки. Детальная панель фиксирована поверх экрана и не меняет сетку.

## Редактор сценариев

- Канонический HACS-фрейм редактора: `282:582`, 1440 × 900 px, Inter.
- Единый поток настройки: `Основное → Когда → Если → Тогда → Доступ`.
- Модальное окно фиксировано поверх интерфейса, не меняет сетку и сохраняет
  понятные состояния ошибок, dirty, disabled и подтверждения.
