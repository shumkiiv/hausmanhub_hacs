# Проверка Hausman for Home Assistant 1.52.238

В выпуске появилась кнопка «Вернуть устройства в климат. контур». Она
показывается только при фактических ручных исключениях и при явном разрешении
сервера. Нажатие использует существующее действие
`return_all_to_automatic`: оно возвращает ручное владение текущего контура,
но не меняет температуру, влажность и не вызывает приборы.

Проверено до публикации:

- Тесты панели: 39 PASS, включая видимость, защищённый запрос, блокировку
  повторного нажатия и перечитывание состояния.
- Полный pytest: 2884 PASS, 4 штатно пропущены, 1293 subtests PASS.
- Полный local release: 2327 unittest, 4 штатно пропущены и 72 pytest PASS.
- Critical coverage: 75% при пороге 75%.
- Browser gate: 50 PASS. Проверены 43 исходные точки, 735 сигнатур,
  708 безопасно выполненных и 27 намеренно заблокированных действий, без
  ошибок, непредусмотренных действий или внешней сети.
- GitHub Actions `34370040394` PASS на `184518c`.

Публикация и установка:

- Публичный GitHub Release `v1.52.238` создан на commit `184518c` без assets.
- Штатный `update.install` применил точную версию `v1.52.238` с уже
  проверенной сегодняшней полной копией. Запрос проверки конфигурации принят,
  затем выполнен один restart.
- После запуска Home Assistant работает на `2026.9.1`, панель отдаёт
  `1.52.238`, climate runtime находится в managed, снимок свежий, активных
  операций нет. Два сенсора планшета снова передают 60% и питание от батареи.
- Новых климатических команд и физического управления приборами не было.
