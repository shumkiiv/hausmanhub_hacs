# Аудит выпуска Hausman for Home Assistant 1.52.184

Дата: 2026-08-27.

## Результат

- Управляемые Node-RED-сценарии больше не показывают техническую секундную
  паузу как единственное пользовательское действие. Вместо ложного счётчика
  редактор показывает динамический план и сохранённое описание устройств.
- Позиция независимой прокрутки колонок сохраняется после изменения поля и
  фоновой перерисовки панели.
- Commit, annotated tag `v1.52.184` и Release без assets указывают на
  `260fb4f`.
- GitHub Actions `33068253295` завершился успешно.
- Production Home Assistant использует installed/latest `v1.52.184`.

## Проверки до установки

- Local release gate: 1734 теста, 4 skipped.
- Browser gate: 19 Chromium-тестов, включая visual и accessibility.
- Регрессионный browser-тест проверяет отсутствие ложного действия, сохранение
  понятного описания и восстановление scrollTop после локальной и фоновой
  перерисовки.
- Пакет HACS, версия, README, branch coverage и границы публичного репозитория
  проверены.

## Резервная копия и установка

- Повторно использован созданный в тот же день protected full backup
  `6d01ac53` в `hassio.local`, размер 882913280 байт.
- Версия установлена точно через update service с `backup=false`.
- Config check принят до установки и после неё, Core перезапущен один раз и
  штатно поднялся.

## Проверка production

- installed/latest равны `v1.52.184`, обновление не выполняется.
- В каталоге 53 сценария, все 53 включены; Scenario Health `healthy`.
- Node-RED 22.0.2 установлен, запущен и подключён; доступны два managed flow.
- Live JS скрывает `safe_placeholder`, показывает `Алгоритм Node-RED` и
  восстанавливает положение трёх колонок. Основной CSS и JS используют cache
  version `1.52.184`.
- Read-only smoke прошёл, максимальная задержка API 102 мс. Физические команды
  дому не отправлялись.
- Все 12 служебных сущностей доступны. В system log нет записей Hausman.

## Откат

Восстановить protected full backup `6d01ac53` либо точно установить
`v1.52.183`, выполнить config check и один restart.
